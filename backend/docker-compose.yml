services:
  db:
    image: postgres
    container_name: linkvault-db
    environment:
      POSTGRES_USER: zainab
      POSTGRES_PASSWORD: 12345
      POSTGRES_DB: linkvault
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
